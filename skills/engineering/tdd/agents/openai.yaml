interface:
  display_name: "TDD"
  short_description: "Drive observable behavior red to green"
  default_prompt: "Use $tdd to implement this observable behavior through a public seam."
policy:
  allow_implicit_invocation: true
