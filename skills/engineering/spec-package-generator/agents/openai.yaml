interface:
  display_name: "Spec Package Generator"
  short_description: "Create a gated AI-ready specification package"
  default_prompt: "Use $spec-package-generator to turn this requirement into the confirmed two-gate specification package with traceable, readiness-checked task prompts."
policy:
  allow_implicit_invocation: true
