interface:
  display_name: "Spec Package Generator"
  short_description: "Create durable, decision-driven implementation specs"
  default_prompt: "Use $spec-package-generator to clarify this requirement one recorded decision at a time, then build its confirmed two-gate implementation package."
policy:
  allow_implicit_invocation: true
