interface:
  display_name: "Spec Package Generator"
  short_description: "Create specs with human-confirmed executable Task slices"
  default_prompt: "Use $spec-package-generator to turn this requirement into a confirmed two-gate package with reviewable capability Tasks and YAML execution Manifests."
policy:
  allow_implicit_invocation: true
