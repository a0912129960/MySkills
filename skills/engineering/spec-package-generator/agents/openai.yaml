interface:
  display_name: "Spec Package Generator"
  short_description: "Create specs with testable vertical task slices"
  default_prompt: "Use $spec-package-generator to turn this requirement into a confirmed two-gate package with testable vertical capability slices and safe parallel development waves."
policy:
  allow_implicit_invocation: true
